general:
  buildTool: 'maven'
  productiveBranch: 'master'
  githubTokenCredentialsId: 'git-token-sumeet'
  verbose: true
stages:
steps:
  codeqlExecuteScan:
    dockerImage: 'securitytestingtools.int.repositories.cloud.sap/piper/codeql:jdk8'
    uploadResults: true
    analyzedRef: 'refs/heads/main'
    repository: 'https://github.com/sumeetpatil/ghas-reference'
    commitId: '{env.GIT_URL}'
    
